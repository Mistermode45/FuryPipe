# Baseline report — 2026-09-25

Starting point for the `claude/furypipe-studio-universal-ai-workspace` track.

| Item | Value | Evidence |
|---|---|---|
| Repository | `Mistermode45/FuryPipe` | `git remote -v` |
| Default branch | `v5-codex-review-clean` | GitHub API |
| Foundation | PR #226 `OPEN + DRAFT`, head `69e2832d1847f7cd71cf1d6f43ab9ae17b180fd0`, base `codex/furypipe-final-validation-closure` @ `eed62617…` | GitHub API |
| Stack | 27 open stacked draft PRs (#199–#226) plus 2 dependabot PRs | GitHub API |
| Hosted CI on foundation | 31/31 checks green (CI 9/9: ubuntu-24.04, macos-14, windows-2025 × Node 22.23.2, 24.21.0, 26.8.2) | check-runs API |
| Package | `furypipe@0.16.0` (not published; npm `latest` 0.15.0); RC tarball SHA-256 `99feb17fc8747e9b5de8a5cf23f58536a5a348d66242bfb62690703a7bbdf08a`, 5 404 064 bytes, 614 files | RC Preparation job |
| Local toolchain | Linux x64, Node 22.22.2, npm 10.9.7, pnpm 10.21.0, git 2.43.0 | command output |
| Typecheck | PASS (main + hosted MCP) | `pnpm run typecheck` |
| Unit/contract tests | 284 files / 3 223 tests PASS, ~86 s | `pnpm test` |
| Build | PASS (tsc library + esbuild `dist/node.js`, `dist/mcp.js`) | `pnpm run build` |
| Package smoke | PASS incl. installed Gateway → MCP stdio | `pnpm run package:smoke` |
| Audit | PASS, no known production vulnerability | `pnpm run audit` |
| FuryBench | PASS, 3 paired rounds, p95 threshold ×1.25, 0 regression | `pnpm run validation:furybench` |
| Graphify | not installed on the host; graphifyy 0.9.67 installed in an isolated venv for evaluation only. `graphify update` (AST only, no LLM) on `src/`: 266 files → 6 519 nodes / 15 136 edges in ~10 s; clustering → 192 communities | local run |
| Master product doc | NOT READ (egress blocked) | `MASTER_DOC_SYNC_LOG.md` |

Existing primitives reused by this track (no rebuild): `codegraph` (native graph
fallback), `agent-fabric` / `agent-runtime` (staged agent harness),
`coding-runtime` (repository/worktree/sandbox/process receipts),
`model-adapter-registry`, provider fabric and governed provider executor,
`knowledge`, `memory-vnext`, `continuous-memory`, `fury-trust`, MCP Direct
(policy, governed execution, durable replay), ACP v1 server/client and A2A
remote adapter, Gateway (auth, sessions, WebChat, automations, channels),
dashboard/Control Plane and Web Studio.
