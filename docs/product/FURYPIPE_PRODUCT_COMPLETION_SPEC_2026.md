# FuryPipe Product Completion Spec 2026

Single definition of done for the `claude/furypipe-studio-universal-ai-workspace` track (mission §20–§21, §119). No numbered phases are added on top of this list; a requirement leaves this file only by reaching DONE with evidence or by an explicit reclassification recorded in `MASTER_DOC_SYNC_LOG.md`.

Source: master product vision 2026-09-25 (read in full from the operator's export; see `MASTER_DOC_SYNC_LOG.md`) and the operator's 2026-09-25 mission prompt. Section references (§) point to the master document.

Status values: NOT_STARTED, PARTIAL (existing primitive, not yet meeting the acceptance criteria), DONE (acceptance met, evidence linked), OPTIONAL_NOT_LIVE_VERIFIED (contract/mocks pass, live service not available).

Release gates (all MUST): P0 = 0, P1 = 0, P2 release-blocking = 0, exact-head CI green on 3 OS × Node 22/24/26, installed-package smoke green, reproducible package, documentation matches code, no PASS without evidence.

| ID | Requirement | Class | Depends on | Acceptance | Test strategy | Status |
|---|---|---|---|---|---|---|
| CORE-01 | FuryProof evidence kernel: receipts (TOOL/AGENT/PATCH/TEST/BROWSER/MODEL/INTEGRATION/APPROVAL), claim states OBSERVED/DERIVED/CLAIMED/VERIFIED, agents cannot self-promote CLAIMED→VERIFIED | MUST | – | A claim without a matching verified receipt stays CLAIMED; bundle digest is deterministic; tampered receipt is rejected | unit + property tests | DONE — src/fury-proof.ts, tests/fury-proof.test.ts |
| CORE-02 | FuryJudge: ACCEPT/REWORK/REJECT/ESCALATE/UNPROVEN from requirements × receipts | MUST | CORE-01 | Missing evidence for a MUST predicate → UNPROVEN; failing test receipt → REWORK; policy violation → REJECT | unit | DONE — FuryJudge in src/fury-proof.ts |
| CORE-03 | FuryIR / Executable Intent Contract (MUST, MUST_NOT, allowed/denied capabilities, budget, success predicates, human gates, task DAG, evidence contract), versioned and strictly validated | MUST | – | Unknown keys, cycles, oversize and unbounded budgets rejected; digest stable | unit | DONE — src/fury-ir.ts, tests/fury-ir.test.ts |
| CORE-04 | Harness Hub: registry + safe discovery (installed, version) for Claude Code, Codex, Gemini CLI, OpenCode, OpenClaw, OpenHands, Goose, Kilo, FuryPipe Native, ACP/A2A generic; never reads credentials | MUST | – | Discovery uses only PATH lookup + `--version` with timeout, no shell; absent tools reported as not installed | unit with fake binaries | DONE (discovery) — src/fury-harness-hub.ts: 9 CLI harnesses + generic ACP and A2A protocol entries (configured endpoints, never executed by discovery); execution adapters: Claude Code (flags verified against CLI 2.1.282 help) and Codex (community-documented flags, UNVERIFIED against a local binary: not installed here); other harnesses refuse with "no execution adapter" |
| CORE-05 | Local model fabric: loopback discovery of Ollama, LM Studio, llama.cpp, vLLM, SGLang, LocalAI, generic OpenAI/Anthropic-compatible; capability probe; hardware discovery kept local; FITS/MAY_BE_SLOW/DOES_NOT_FIT | MUST | – | Probes only allow-listed loopback/LAN targets, bounded bodies, timeouts; classification deterministic | fake HTTP servers | DONE — src/fury-local-fabric.ts; SGLang/LocalAI/Jan verified only through the OpenAI-compatible path |
| CORE-06 | FuryDispatcher: OFF/SINGLE/AUTO/MANUAL/PIPELINE/PARALLEL/COUNCIL/RACE/REVIEW_CHAIN/SPECIALISTS/LOCAL_CLOUD_HYBRID/LOCAL_ONLY/CUSTOM_GRAPH; AUTO may return DISPATCH_BENEFIT=LOW; child authority ≤ parent; privacy boundary enforced | MUST | CORE-03,04,05 | Plans are deterministic for a given registry; LOCAL_ONLY never selects a cloud provider; escalated child authority rejected | unit | DONE — src/fury-dispatcher.ts plans; plans execute end to end through src/fury-run.ts (writers in worktrees → integrator → readers → judge), tested with a fake harness and real git |
| CORE-07 | Worktree isolation: 1 writer agent = 1 git worktree, cleanup of orphans | MUST | CORE-06 | Two writers never share a worktree; orphan worktrees detected and removed | integration with real git | DONE — src/fury-writer-pool.ts with real git worktrees |
| CORE-08 | FuryPlanner: requirements → task DAG with owners, budgets, stop conditions, human gates | MUST | CORE-03,06 | Planner output validates as FuryIR | unit | DONE — src/fury-planner.ts (intent + planned files → validated FuryIR DAG with owners, gates and budgets), tests/fury-planner.test.ts |
| CORE-09 | FuryIntegrator: ordered patch application with conflict detection, never blind merge | MUST | CORE-07 | Conflicting patches stop with a conflict report; clean patches applied in sequence with receipts | integration with real git | DONE — src/fury-integrator.ts (ordered merges in a dedicated worktree, conflict stop, verification receipts), tests/fury-integrator.test.ts with real git |
| CORE-10 | FuryGraph provider interface + Graphify provider (real graph.json schema) + native codegraph fallback; blast radius | MUST | – | Graphify fixture parsed; missing Graphify falls back to native; affected set computed by reverse traversal | unit with real Graphify fixture | DONE — src/fury-graph.ts, real Graphify 0.9.67 fixture |
| CORE-11 | FuryContext Compiler: task context capsule with source, reason, digest, scope, expiry; never drops MUST constraints | MUST | CORE-03,10 | Over-budget capsule drops lowest-priority items only; MUST constraints always present | unit + FuryBench context case | DONE — src/fury-context-compiler.ts; FuryBench context case in docs/evidence/FURYBENCH_STUDIO_2026-09-25.md (mean recall of co-changed files 0.282 vs 0.201 same-folder baseline, budget matched) |
| CORE-12 | Mission Control model: per-agent state, budget, receipts; actions PAUSE/STOP/RETRY/CHANGE_MODEL/APPROVE/DENY | MUST | CORE-06 | State transitions validated; illegal transitions rejected | unit | DONE — src/fury-mission-control.ts (validated transitions, budgets, START/PAUSE/RESUME/STOP/REDIRECT/RETRY/CHANGE_MODEL/HANDOFF/APPROVE/DENY), tests/fury-mission-control.test.ts |
| CORE-13 | FuryReplay: task record, fork with different runtime/model | MUST | CORE-01,03 | Replay record digest stable; fork preserves inputs and changes only the declared axis | unit | DONE — hash-chained FuryReplay with verify/fork/explain in src/fury-mission-control.ts |
| UX-01 | FuryPipe Studio shell: Chat/Cowork/Code/Agents/Automations + secondary nav; dashboard under Settings › Advanced › Control Plane | MUST | CORE-04,05 | Served by the runtime; keyboard navigable; no console errors; Chromium/Firefox/WebKit QA | browser QA + a11y | PARTIAL — shell served at /, 17 views, Control Plane at /control-plane; Chromium QA local; Firefox/WebKit run in hosted Cross-Browser QA; human screen-reader pass pending |
| UX-02 | Chat: conversations, model/provider/runtime/local-cloud indicators, model switch, retry with another model | MUST | UX-01,CORE-05 | Indicators always visible; switch recorded in replay | browser QA | DONE (local runtimes) — persisted conversations (src/studio/studio-chats.ts), branch from any answer, retry with another model as a branch, model/provider/locality recorded on every answer in the conversation record and shown in the header. Not in Studio yet (master §7, recorded as gaps in MASTER_DOC_SYNC_LOG): cloud models (they stay in the governed Gateway WebChat), attachments, compare/council, export/import, cost/tokens display |
| UX-03 | Cowork: file/folder tasks with READ/WRITE/EXECUTE/NETWORK/EXTERNAL_ACTION permissions and ALLOW/ASK/DENY | MUST | UX-01,CORE-06 | Denied capability never executes; ASK surfaces an approval | browser QA + unit | DONE — Cowork runs real agents: chosen permissions flow into the contract; DENY reaches every agent (WRITE denied → read-only review plan), ASK must be approved in the UI before start (409 approval-required otherwise), EXTERNAL_ACTION never ALLOW; tests/studio-api.test.ts + browser QA |
| UX-04 | Code: repo explorer, diff, tests, worktrees, agents | MUST | UX-01,CORE-07 | Diff and test receipts visible per worktree | browser QA | DONE (read-only) — Code view: graph, blast radius, project file explorer (realpath-confined), worktree list with changed-file counts, per-worktree diff and the Mission Control agents/receipts that ran there (src/studio/studio-code.ts); editing and terminal are out of this view by design (agents write in worktrees) |
| UX-05 | Agents / Mission Control view | MUST | UX-01,CORE-12 | Live state from the runtime API | browser QA | DONE — Mission Control view with real, confirmed, judge-gated runs (local runtimes unless cloud is allowed), live worker state and STOP; browser QA |
| UX-06 | Automations / FuryFlow builder with deterministic vs agentic zones | MUST | UX-01 | Flow validates as a DAG; zones rendered | browser QA + unit | DONE — src/fury-flow.ts (23 node types, deterministic/agentic zones, dry-run with checkpoints) and the Studio Automations canvas; browser QA |
| CORE-14 | Budget governance modes FAST, BALANCED, QUALITY, BUDGET, LOCAL-FIRST, PRIVATE, CUSTOM mapped to dispatcher ranking and privacy (§28) | MUST | CORE-06 | Each mode changes ranking/filters deterministically; PRIVATE = local-only | unit | DONE — FURY_BUDGET_PROFILES FAST/BALANCED/QUALITY/BUDGET/LOCAL_FIRST/PRIVATE/CUSTOM in src/fury-dispatcher.ts (PRIVATE = local only), tests/fury-dispatcher.test.ts |
| CORE-15 | Predicted vs actual impact: blast radius before patch compared with the changed dependency region after patch; Judge can require tests for uncovered neighbours (§47.5) | MUST | CORE-10, CORE-02 | Delta lists unexpected files and untested neighbours | unit | DONE — furyImpactDelta + furyImpactRequirements in src/fury-graph.ts, applied by FuryRun before judging |
| SHOULD-04 | Graphify refresh of changed files after agent patch/merge/checkout (§47.2) | SHOULD | CORE-10 | Explicit, shell-free, bounded | unit | PARTIAL — refreshGraphify (explicit, shell-free, bounded) exists in src/fury-graph.ts; it is not triggered automatically after merge/checkout and not exposed in Studio |
| UX-07 | Progressive UX: Simple (chat, model, message) → Power → Engineer → Expert without exposing DAG/receipts to simple users (§37) | MUST | UX-01 | Simple mode hides expert surfaces; switch persisted per viewer | browser QA | DONE — Simple/Power/Engineer/Expert selector in Studio, persisted per viewer; browser QA |
| PLAT-01 | Skills Hub (discovery across .furypipe/.claude/.agents/.opencode skills, metadata, enable/disable/pin) | MUST | – | Existing skill-registry reused; discovery bounded and symlink-safe | unit | DONE — src/fury-skill-hub.ts (checksums, pin/enable, governance, versioned install/rollback/compare, runtime-compatible auto-select; .opencode/skills discovered) + Studio Skills view |
| PLAT-02 | MCP Hub (installed/local/remote, health, permissions) | MUST | – | Existing MCP Direct reused | unit | DONE — src/fury-mcp-hub.ts (Claude/Cursor/VS Code/OpenCode/Codex/FuryPipe configs, secret redaction, per-tool ALLOW/ASK/DENY/READ_ONLY, credential-free MCP Direct health probes) + Studio MCP view |
| PLAT-03 | Memory (scopes, why-retrieved, source, age, confidence) | MUST | – | Existing Memory VNext reused | unit | DONE — Studio Memory over Memory VNext (src/studio/studio-memory.ts): scope, source, age, confidence, why recalled; encrypted config required, off otherwise |
| PLAT-04 | Knowledge/RAG (ingestion, chunking, hybrid retrieval, citations) | MUST | – | Existing knowledge index extended | unit | DONE — src/fury-knowledge.ts (bounded incremental ingestion, heading/line chunking, BM25 + loopback embeddings + RRF, line citations) + Studio Knowledge view |
| PLAT-05 | FuryWeb (search/fetch/extract/crawl/browser split, adapter-first) | MUST | – | Fetch never launches a browser; SSRF defenses | unit | DONE — src/fury-web.ts (SEARCH adapter-only, FETCH without browser with pinned SSRF-safe sockets and revalidated redirects, EXTRACT, MAP, robots-aware CRAWL; BROWSER/INTERACT stay in the governed browser runtime) + Studio Web view |
| PLAT-06 | Integration Fabric registry (MCP/OpenAPI/webhooks) | MUST | PLAT-02 | Registry entries carry auth, permissions, trust | unit | DONE — src/fury-integrations.ts (MCP + OpenAPI + webhooks with auth names, capabilities, default decision, trust; manifests with secret values rejected) + Studio Integrations view |
| QA-01 | Security regressions for new boundaries (SSRF, path traversal, symlink, injection, authority escalation, oversized payloads) | MUST | all CORE | Each new boundary has a negative test | unit | DONE — every boundary added on this track has a negative test, listed with its test file in docs/security/FURYPIPE_TRUST_MODEL.md |
| QA-02 | Installed-package smoke covers new runtime surfaces | MUST | UX-01 | npm pack → install → CLI → Studio route | package smoke | DONE — clean-room smoke (npm pack → install → CLI → runtime) checks Studio shell, Control Plane, harnesses, Skills, MCP, integrations, knowledge, memory-off default, web SSRF refusal, conversation persistence, code explorer and cross-origin POST refusal |
| QA-03 | Cross-OS CI 9/9 green on exact head; package reproducible | MUST | all | Hosted checks | CI | PENDING — hosted cross-OS CI must be green on the final exact head (recorded in the PR body when it is) |
| QA-04 | FuryBench cases for dispatcher, context compiler, local inference fit | MUST | CORE-06,11,05 | Paired A/B, same fixtures, budget matched | FuryBench | DONE — scripts/furybench-studio.ts, docs/evidence/FURYBENCH_STUDIO_2026-09-25.md; local-fit case NOT_EXECUTED here (no local model server) |
| SHOULD-01 | Voice/realtime adapter (LiveKit-style) reusing model fabric and permissions | SHOULD | – | Disabled by default; no kernel impact | unit | PARTIAL (media-realtime-voice exists) |
| SHOULD-02 | Marketplace with signed metadata | SHOULD | PLAT-01,02 | Unsigned component shown as untrusted | unit | NOT_STARTED |
| SHOULD-03 | Multichannel continuation without permission widening | SHOULD | – | Channel never widens scopes | unit | PARTIAL (gateway channels) |
| OPT-01 | FuryTeleport, FuryMultiverse, Shadow Twin, Epistemic Graph, Outcome Loop, Self-Science, Agent Market, Compute Mesh, Failure Genome | OPTIONAL (FRONTIER_BET) | CORE | Each needs its own benchmark before any claim | – | FRONTIER_BET (master §46, §48) — not release criteria. Status per item: FuryIR, FuryProof, Intent Kernel (= FuryIR contract), Context Compiler: implemented as core (CORE-01/03/11). Partial primitives only: Time Machine (FuryReplay fork), Determinism Envelope (FuryFlow zones + stochastic surface), Trust Fabric (trust model + skill/MCP trust), Zero-Trust Agent State (CLAIMED vs VERIFIED receipts). Not started: Teleport, Multiverse, Shadow Twin, Skill Compiler, Epistemic Graph, Collective Cortex, Self-Science, Outcome Loop, Failure Genome, Agent Market, Adaptive Workspace, Compute Mesh. None is a FURY_DIFFERENTIATOR until implemented, tested and benchmarked |
| OOS-01 | Merge, tag, release, publish, deploy, paid provider calls, real external credentials | OUT OF SCOPE without explicit operator authorization | – | Never executed by this track | – | N/A |
